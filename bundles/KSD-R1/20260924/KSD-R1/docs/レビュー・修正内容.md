# レビューと修正内容

作成日: 2026-09-24 01:50 JST
作成者: Codex (GPT-6)

[READMEに戻る](../README.md)

本番の動作を維持する方針を継続し、復元データ・計画・配布物の整合性をレビューしました。以下は本番の数式不具合ではなく、検証・引渡しの問題に対する修正です。

## 確認した問題と対応

| 重要度 | 問題と影響 | 修正内容 |
|---|---|---|
| 中 | 前回の計画書ZIPにはExcelがなく、Excelを含めた一式として使えない | 参照Excelを同梱し、テキストbundleにも全バイトの復元payloadを追加 |
| 中 | 検証処理が作業者PCの絶対パス・別フォルダーのデータに依存 | 同梱物だけを使う標準Pythonの読み取り専用検証へ変更 |
| 中 | 依存先の検査が件数中心で、21セルの内訳が誤っていても検出できない | 実Excelの936式から依存先を再計算し、セル集合全体を照合 |
| 中 | 「元SHAと変更前式に一致」と「適用済みへ再実行して差分0」の関係が曖昧 | 未適用・適用済み・混在状態の判定を計画に明記し、混在は停止 |
| 中 | 既存Excelへの復元時の扱いとbundleの改行変換後の復元性が未定義 | 同一ハッシュなら再利用、異なれば上書き拒否。Gitに格納するbundleから復元検証 |
| 中 | 今回作成した復元ツールがPowerShell 5.1で復元先省略時に失敗 | スクリプト本体で既定値を解決する方式へ修正。説明書どおりの引数なし復元を5.1／7両方の回帰試験へ追加 |

## 維持する推奨

現行の比較基準と共通数式の保守を整備し、条件を満たす場合だけ検索キーを1セルへ分離します。対象は8既存セルと補助1セルです。VLOOKUPの10回、検索範囲、列番号、左右分岐、IFERROR、演算順序は維持します。

事前計算表・係数化・カスタムマスターへの全面移行は、性能改善の実測根拠がなく維持費と回帰範囲が増えるため見送ります。

## 検証の区分

配布物のファイル整合性、数式、bundle復元と既存ファイル保護の結果は同梱の `evidence` に保存します。別実行のCodex CLIレビューを実施し、重大な指摘は修正して再確認します。最新の実施結果は[検証結果](検証結果.md)を参照してください。

Excelの変更は0件です。同梱Excelは2026-09-18の復元ブックと同一バイトです。元の復元時にはExcelでの再計算・保存再オープン等を確認していますが、それを本番iPhoneの受入に置き換えません。

## 未実施と残る判断

本番コピーの設定照合、補助セルの実配置、Designerへの取込み、iPhoneの旧新比較、本番反映・切戻しは未実施です。本番定義がない状態で補助セルやindexを推測して変更しません。

共有先の既存GitHubリポジトリは2026-09-24時点で公開設定でした。ローカルでの格納・コミットと、GitHubへのプッシュ／公開は別の状態として報告します。
